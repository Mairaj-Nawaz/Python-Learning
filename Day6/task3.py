# Take two lists in a function and convert them into a set separtely
# Then merge both sets and return the merged set
# Requirements:
# Define a function merge_sets(list1, list2) that:
# Takes two lists list1 and list2 as input.
# Converts both lists into sets.
# Merges both sets.
# Returns the merged set.